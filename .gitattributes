# Normalize line endings to LF in the repo; let Git handle the working copy.
* text=auto eol=lf

# Treat binary assets as binary (no EOL conversion).
*.jpeg binary
*.jpg binary
*.png binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary
